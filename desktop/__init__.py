"""PySide6 desktop demonstration of the Vision Assistant SDK.

This package is a thin *demo* over :mod:`src.engine`. It is not part of the SDK and
host camera software would not use it — it exists to show the engine working across
the three input modes (live RTSP, video, image). Launch with ``python -m desktop``.
"""
